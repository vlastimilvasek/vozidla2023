import { TelefonFormatPipe } from './telefon-format.pipe';

describe('TelefonFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new TelefonFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
