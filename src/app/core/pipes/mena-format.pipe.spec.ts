import { MenaFormatPipe } from './mena-format.pipe';

describe('MenaFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MenaFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
