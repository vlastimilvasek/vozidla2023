import { SpolFormatPipe } from './spol-format.pipe';

describe('SpolFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new SpolFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
