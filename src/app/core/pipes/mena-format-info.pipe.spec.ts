import { MenaFormatInfoPipe } from './mena-format-info.pipe';

describe('MenaFormatInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new MenaFormatInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
